{
  "name": "trisync-license-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for TRI-SYNC license issuance and validation",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
