{
  "name": "tri-sync-worker",
  "version": "1.0.0",
  "description": "TRI-SYNC Cloudflare Worker — Stripe webhook → KV → Resend email",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
